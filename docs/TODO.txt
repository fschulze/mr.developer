* use pexpect for better interaction with the called processes
* rethink syntax for branches/revisions and project repo
* config for git repos
  - branches from different remotes
* personal variants (.mr.developer.cfg / default.cfg)
  - git-svn for svn
* gitify of svn repos
